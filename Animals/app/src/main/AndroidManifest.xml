<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="pioneers.safwat.animals">
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name="pioneers.safwat.animals.AnalyticsApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".Present"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CardDemoActivity"/>
<activity android:name=".CardDemobordse"/>
        <activity android:name=".CardDemopitse"/>
        <activity android:name=".CardDemoWilde"/>
<activity android:name=".CardDemoActivitya"/>
        <activity android:name=".CardDemobordsa"/>
        <activity android:name=".CardDemoWilda"/>
        <activity android:name=".CardDemopitsa"/>



    </application>

</manifest>